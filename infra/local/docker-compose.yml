services:
  # O protótipo atual usa Supabase remoto/configurado por env.
  # Adicione serviços locais aqui quando o banco ou API forem internalizados.
  placeholder:
    image: alpine:3.20
    command: ["sh", "-c", "echo infra local pronta para evoluir"]
